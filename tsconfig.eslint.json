{
    "extends": "./tsconfig.json",
    "include": [
        // 👇️ add all the directories and files
        // that you want to lint here
        "src",
        "config",
        "scripts",
        "json-server",

        // add all files in which you see
        // the "parserOptions.project" error
        ".eslintrc.yml",
        "**/*.config.ts" // "webpack.config.ts"
    ]
}
